---
name: "auto-demo-recorder"
description: "独立演示录屏工具，提供Web控制面板，用户手动点击「开始录屏」按钮才启动。模拟TRAE界面输入指令、窗口置顶展示结果、支持自动/手动模式。Invoke ONLY when user explicitly asks to record a demo video or launch the recorder panel."
---

# Auto Demo Recorder (演示录屏工具)

**版本**: v3.0
**适用系统**: Windows 10/11

> **重要**: 此工具完全独立，不会随SKILL执行自动触发。
> 用户必须手动启动Web控制面板并点击「开始录屏」按钮才会开始录制。

---

## 触发条件

**仅在以下情况调用**:
- 用户明确说"录屏"、"录制演示视频"、"打开录屏面板"
- 用户说"帮我做个演示视频"
- 用户要求展示SKILL的运行效果录像

**不要在以下情况调用**:
- 用户只是执行4G5G指标自动通报 → 正常执行SKILL即可
- 用户查看输出结果 → 直接打开文件即可
- 用户没有提到录屏/视频相关需求

---

## 启动方式

```bash
cd auto-demo-recorder
python server.py
```

启动后浏览器访问 `http://localhost:8765`

---

## Web 控制面板功能

| 功能 | 说明 |
|------|------|
| 模式选择 | 自动模式 / 手动模式，在页面上切换 |
| 参数配置 | 指令文字、脚本路径、输出目录、各步停留时间 |
| 开始/停止 | 点击「开始录屏」才启动，可随时「停止录屏」 |
| 实时状态 | 显示当前步骤、进度条、已录制时长 |
| 手动下一步 | 手动模式下，页面上显示「下一步」按钮，点击切换 |
| 输出管理 | 录制完成后可直接「播放视频」或「打开输出目录」 |

---

## 录制流程（点击开始后执行）

```
1. 桌面开场画面
2. 展示源数据目录（4G / 5G）
3. TRAE任务界面 → 显示"用户输入：执行4G5G指标自动通报" → 运行脚本
4. 等待SKILL执行完成（自动检测输出文件）
5. 依次展示所有输出结果（新窗口置顶覆盖上一个）
   4G总表 → 4G计算 → 5G总表 → 5G计算
   → 小区组看板 → 汇总看板 → 合并大图 → 文字通报
6. 结束录制，保存MP4
```

---

## 两种模式

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| **自动模式** | 每步按配置秒数自动停留 | 快速生成成片、无人值守 |
| **手动模式** | 每步在网页上点「下一步」才继续 | 需配合鼠标操作、讲解、特写 |

---

## 文件结构

```
auto-demo-recorder/
├── server.py          # Web后端（HTTP服务 + 录屏API）
├── index.html         # Web前端控制面板
├── record_demo.py     # 命令行版录屏脚本（可选，独立使用）
├── SKILL.md           # 本文档
├── README.md          # GitHub项目说明
├── LICENSE            # MIT
└── .gitignore
```

---

## 依赖

```bash
pip install opencv-python mss numpy
```

---

## 配置项（Web面板上可直接修改）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 指令文字 | 执行4G5G指标自动通报 | TRAE界面显示的用户输入 |
| 脚本路径 | ../full_process.py | 目标SKILL脚本 |
| 输出目录 | C:\zhibiao\pic_result | 视频和结果输出目录 |
| 开场停留 | 3秒 | 桌面画面展示时间 |
| 源数据停留 | 4秒 | 源目录展示时间 |
| 指令展示 | 3秒 | TRAE界面输入展示 |
| 结果停留 | 5秒 | 每个输出文件展示时间 |
| 结束停留 | 3秒 | 结束画面 |

---

## API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 返回控制面板页面 |
| `/api/status` | GET | 获取当前录制状态 |
| `/api/config` | GET | 获取当前配置 |
| `/api/start` | POST | 开始录制 |
| `/api/stop` | POST | 停止录制 |
| `/api/next-step` | POST | 手动模式：进入下一步 |
| `/api/open-folder` | POST | 打开输出目录 |
| `/api/open-video` | POST | 播放生成的视频 |
